#!/usr/bin/env bash
### BEGIN INIT INFO
# Provides:          twitposter
# Required-Start:    $network $remote_fs $syslog $time
# Required-Stop:     $network $remote_fs $syslog $time
# Default-Start:     2 3 4 5
# Default-Stop:      0 1 6
# Short-Description: Start/stop twitposter daemon
# Description:       Starts and stops the twitposter daemon which posts status
#                    updates to Twitter on a semi-random schedule.
### END INIT INFO
